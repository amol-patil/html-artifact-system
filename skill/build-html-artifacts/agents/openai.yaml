interface:
  display_name: "HTML Artifact Kit"
  short_description: "Create polished, portable HTML artifacts"
  default_prompt: "Use $build-html-artifacts and HTML Artifact Kit to create a polished, self-contained HTML artifact."
policy:
  allow_implicit_invocation: true
