# Portable prompt

Create a polished, professional, self-contained HTML artifact for the request below. Follow the attached `SKILL.md` and load the referenced guidance for the selected artifact type. Return or save one complete HTML document with inline CSS and JavaScript, no framework runtime, no production CDN dependency, accessible interactions, responsive layout, and intentional print behavior where relevant.

Request:

> Replace this line with the artifact brief.
